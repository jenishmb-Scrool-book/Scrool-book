<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <!--
        INTERNET здесь намеренно НЕТ.

        Приложение целиком офлайновое: страница отдаётся из assets через
        WebViewAssetLoader, сеть не нужна ни одному плагину. Отсутствие
        разрешения — единственное утверждение о приватности, которое
        пользователь может проверить сам, не веря нам на слово.

        Обратная сторона: если что-то в WebView попробует выйти наружу, оно
        молча не загрузится. Появится внешний ресурс — разрешение придётся
        вернуть, и вместе с ним переписать раздел «Data Safety».

        Уведомления добавляют POST_NOTIFICATIONS, RECEIVE_BOOT_COMPLETED и
        WAKE_LOCK — они приходят из манифеста плагина при сборке.
    -->
</manifest>
