// Название продукта — одной строкой на всё приложение.
//
// Это не строка интерфейса, поэтому в i18n её нет: имя одинаково в обоих
// языках. Но живёт оно не только в JS — то же самое слово лежит в <title>
// страницы, в strings.xml (его читает лаунчер Android) и в capacitor.config.json.
// Разъезжаются они молча: на телефоне подписано одно, в магазине другое,
// в уведомлении третье. За совпадением следит name.test.js — он читает те
// файлы с диска и сверяет их между собой.
export const APP_NAME = 'Scrool Book';
