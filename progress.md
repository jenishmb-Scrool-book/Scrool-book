# Progress

## Session 2026-09-06
- Каталог проекта был пуст. Стек выбран: один index.html, ванила, без зависимостей.
- Созданы task_plan.md / findings.md / progress.md.
- Написан index.html: домашний экран, библиотека, чанкер, 4 рендерера.
- Далее: проверка в браузере.

### Проверено в браузере (375x812)
- Чанкер: 6 ассертов (абзацы, пустое, лимит длины) — проходят. Повтор: `index.html?test`.
- Клипы: карточка ровно в высоту экрана, скролл на 4 карточки → курсор 4.
- Чаты: подхватили позицию из клипов, «Дальше» двигает курсор.
- Лента: старт с pos-1, скролл двигает курсор, выравнивание по верху поста.
- Видео: скролл списка курсор НЕ двигает (браузишь ≠ читаешь), открытие ролика — двигает.
- Плеер, удаление книги, восстановление cur после удаления — ок.
- Пользовательский текст экранируется (`<img onerror>` не исполняется).

## Session 2026-09-06 — порт на React + Capacitor

### Task 1 (ядро) — готово, коммит fc34ea2
42 теста зелёные: chunk 11, storage 13, store 18.
Решения агента, которые стоит помнить:
- Предложение длиннее max выходит одним куском (рубить по символам = рвать слово).
- addBook резолвится в null и пишет в `error`, а не бросает — экраны не оборачиваются в try/catch.
- Откат текста, если мета не записалась: иначе в хранилище остаётся осиротевший блоб.
- Не-квотные ошибки записи (SecurityError в приватном режиме) не переодеваются в StorageFullError.
- openBook защищён токеном от гонки двух открытий подряд.

### Замечания к чужим файлам, поднятые агентом ядра — В РЕВЬЮ-ГЕЙТ
1. `src/screens/Library.jsx` — `.catch()` на addBook не сработает никогда (она не реджектится).
   Нужно `.then(id => { if (id) go('home') })`, иначе при переполнении уводит на Home как при успехе.
2. `src/__store_stub.jsx` и `vite.stub.config.mjs` — временные, удалить перед интеграцией.
3. `n` книги пересчитывается при каждом openBook — экранам не кешировать, брать `current.n`/`chunks.length`.
4. Пока openBook грузит текст, `chunks` ещё от прошлой книги — не держать индексы между сменами книги.
