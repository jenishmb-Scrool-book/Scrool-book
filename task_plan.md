# Скролл — читалка в виде телефона

## Goal
Читалка для людей с СДВГ / привыкших к вертикальному контенту.
Пользователь вставляет свой текст (книгу), приложение показывает его как
экран телефона с «приложениями»: чаты, клипы, лента, видео.
Один и тот же текст, разные обёртки, общий прогресс чтения.

## Core insight
Текст режется на фрагменты, курсор один и общий для всех экранов.
Каждое «приложение» — это просто другой рендерер над одним и тем же текстом.
Переключился с клипов на чаты — продолжил с того же места.

**Уточнение от 2026-09-06:** курсор — это **смещение в символах**, а не индекс
фрагмента. Причина: у шести движков разные размеры фрагмента, и один и тот же
индекс означал бы разные места книги. Смещение переживает и разную нарезку,
и смену размера шрифта.

## Next Step
Этап 0, шаг 3: вынести все цвета в CSS-переменные и завести светлую палитру
(`src/styles.css`), затем шаг 4 — словарь строк, шаг 5 — разделить `Library.jsx`.
Шаги 1–2 готовы, коммит 2f3b933.

## Phases

### Phase 1 — Решения по стеку
**Status:** complete
- Один файл `index.html`, ванильный JS, ноль зависимостей, ноль сборки.
- Хранение: localStorage (мета отдельно от текстов книг).
- Вертикальная лента — нативный CSS `scroll-snap-type: y mandatory`, не библиотека.
- Названия/иконки «приложений» — свои (Чаты, Клипы, Лента, Видео), без чужих логотипов.

### Phase 2 — Ядро
**Status:** complete
- Домашний экран с иконками + виджет «продолжить».
- Библиотека: вставить текст / загрузить .txt / выбрать / удалить книгу.
- Чанкер (абзацы → предложения, лимит ~280 симв).
- Курсор + сохранение прогресса.

### Phase 3 — Рендереры
**Status:** complete
- Чаты: чанки как входящие сообщения, кнопка «дальше».
- Клипы: полноэкранные карточки, scroll-snap, отслеживание позиции через IntersectionObserver.
- Лента: посты, текст на градиенте.
- Видео: список «роликов» + плеер.

### Phase 4 — Проверка
**Status:** complete
- Открыть в браузере, пройти все 4 экрана, проверить сохранение позиции.
- Самопроверка чанкера по `?test`.

### Phase 5 — Порт на React + Capacitor под Android
**Status:** complete
Решение пользователя: React (не ваниль), публикация через Android Studio.
Ванильный прототип сохранён в `www_backup/vanilla-prototype.html` как референс поведения.
План с контрактами: `docs/plans/2026-09-06-android-react-port.md`.
Харнесс по `subagent-driven-development` + `dispatching-parallel-agents`:
- Task 1 — ядро (чанкер, хранилище, стор) — агент, TDD
- Task 2 — экраны на React — агент
- Task 3 — Android/Capacitor обвязка — агент
- Task 4 — ревью-гейт — агент
Границы владения файлами непересекающиеся, контракты зафиксированы оркестратором заранее.

### Phase 6 — Интеграция и сборка
**Status:** complete

### Phase 7 — Персонализация домашнего экрана
**Status:** complete
Обои из галереи, искажённые названия приложений, индикатор страницы. Коммит 4741c5b.
`npm test`, `npm run build`, прогон в браузере, `npx cap sync android`, инструкция для Android Studio.

### Phase 8 — Этап 0: ядро (последовательно, без агентов)
**Status:** in_progress
Всё, что трогает все файлы сразу и потому не параллелится.
1. Позиция в символах вместо индекса фрагмента (`chunk`, стор, `useCardWindow`, 42 теста).
2. Нарезка с параметром размера — разные размеры фрагмента для разных движков.
3. Токены тем: все цвета в CSS-переменные, светлая и тёмная палитры.
4. Словарь строк: все тексты из компонентов в один файл.
5. Разделить `Library.jsx` на библиотеку и настройки — иначе на Этапе 1 в него упрутся несколько агентов.

Пункты 3–5 делаются здесь, а не позже: после шести движков они втрое дороже.

### Phase 9 — Этап 1: параллельные агенты
**Status:** pending
Контракты фиксируются до запуска. Области непересекающиеся.
| Агент | Область | Файлы |
|---|---|---|
| A | Парсеры fb2 и epub | `src/lib/fb2.js`, `src/lib/epub.js`, `Library.jsx` |
| B | Шесть движков и фейковые данные | `src/screens/*`, `src/lib/fake.js` |
| C | Индикатор, страница ~1800 знаков, «осталось N мин» | `src/ui/Progress.jsx`, `src/lib/pace.js` |
| D | Уведомления | `src/native.js`, `Settings.jsx` |

### Phase 10 — Этап 2: сборка
**Status:** pending
Экран навигации по главам (зависит от A), интеграция, ревью, проверка на устройстве.

### Phase 11 — Этап 3: подготовка к публикации
**Status:** pending
Иконка (владелец) · политика на GitHub Pages · Data Safety · keystore ·
закрытое тестирование 14 дней / 12 человек. Собирать людей начинать параллельно с разработкой.

## Decisions Made
| Решение | Почему |
|---|---|
| Один HTML-файл, без фреймворка | Нечего собирать, открывается двойным кликом, легко переносится в PWA/Capacitor потом |
| localStorage, не IndexedDB | Лимит ~5 МБ хватает на книгу; IndexedDB — когда упрёмся |
| Общий курсор для всех «приложений» | Это и есть фича продукта, а не 4 отдельные читалки |
| Свои названия вместо WhatsApp/TikTok | Товарные знаки; вёрстка всё равно узнаваемая |
| ~~Без EPUB~~ → txt + fb2 + epub | Отменено 2026-09-06: аудитория русскоязычная, книги лежат в fb2. PDF — никогда |
| Позиция в символах, не в индексе | Разные размеры фрагмента у шести движков; переживает и смену шрифта |
| Шесть движков на восемь иконок | Восемь отдельных рендереров утроят цену каждой правки; разницу даёт размер фрагмента, не палитра |
| Фейковые данные детерминированно из индекса | Меняющиеся счётчики ощущаются как поломка; ноль картинок в APK |
| Индикатор — условная страница ~1800 знаков | Номер фрагмента прыгает при переключении экранов, процент не отвечает «сколько листать» |
| `allowBackup="true"` оставлен | Бэкап хранит не книги, а позицию: книгу переимпортируешь, «340 из 700» — нет |
| Разрешение на уведомления после первой главы | Системный диалог показывается один раз за всё время; отказавшийся на старте не вернётся |
| Бесплатно, без рекламы и бэкенда | Нет ни одного пользователя — любая монетизация может оказаться выброшенной работой |

Полный разбор с обоснованиями и отклонёнными вариантами: `docs/plans/2026-09-06-scope-decisions.md`.

## Errors Encountered
| Error | Attempt | Resolution |
|---|---|---|
| Агенты «экраны» и «Android» упали на лимите сессии (429) | 1 | Не перезапускал: код уже лежал на диске почти целиком. Доделал сам — дешевле холодного агента |
| `Library.jsx`: `.catch()` на `addBook` не срабатывает | 1 | `addBook` резолвится в `null`, а не реджектится. Решение по `id` |
| `Library.jsx`: `setText('')` до сохранения | 1 | Текст терялся при переполнении. Чистка поля перенесена в ветку успеха |
| Демо-текст первого запуска потерян при порте | 1 | `src/seed.js` + эффект в `App.jsx` (не в сторе — там 18 тестов) |
| Скриншотилка браузера трижды отвалилась по таймауту | 3 | Проверял через замеры DOM; `zoom` в итоге снял корректный кадр |
| Весь скрипт падал на первой строке | 1 | `localStorage` бросает SecurityError на opaque origin (превью, приватный режим) — обёртка LS с фолбэком в память |
| `open()` в inline-onclick стирал страницу | 1 | В inline-обработчиках `open` резолвится в `document.open` — переименовано в `openBook` |
| IntersectionObserver не двигал курсор | 2 | Заменён обычным scroll-хендлером: карточка ровно в высоту экрана, наблюдатель избыточен |
| sed-якорь совпал с CSS-комментарием, файл раздублировался | 1 | Файл перезаписан целиком; впредь якоря только уникальные |
| Промах скролла на ~81px в ленте/видео | 1 | `offsetTop` считался от `.screen`; добавлен `position:relative` на `.body` |
